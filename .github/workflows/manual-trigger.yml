name: Disparar NoWorryPark manualmente

on:
  workflow_dispatch: {}

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - name: Llamar al endpoint /trigger del Worker
        run: |
          curl -sS -f "${{ secrets.NOWORRYPARK_URL }}/trigger?key=${{ secrets.MANUAL_TRIGGER_KEY }}"